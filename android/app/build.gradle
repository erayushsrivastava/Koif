plugins {
    id "com.android.application"
    // id "org.jetbrains.kotlin.android"   // Match your system version
    id "dev.flutter.flutter-gradle-plugin"
    id "org.jetbrains.kotlin.android" 
}

android {
    namespace = "com.example.koif"
    compileSdk = 34 // Set this to the desired SDK version
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'

    }

    defaultConfig {
        applicationId = "com.example.koif"
        minSdk = flutter.minSdkVersion
        targetSdk = 34 // Set this to the desired SDK version
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}